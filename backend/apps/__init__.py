"""Local Django apps namespace.

M0 ships only `apps.health` to validate the project boots end-to-end.
M1+ adds: users, workspaces, contacts, companies, deals, tasks, notes,
activities, pipelines, integrations, automations, agents, ai.
"""
