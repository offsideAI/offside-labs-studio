// @offside/api-client — typed client generated from the Django OpenAPI schema.
//
// The Swift client mirror lives in frontend-ios/Generated/ (also generated
// from the same OpenAPI source via tools/openapi/sync-ios.sh).
//
// Run `pnpm --filter @offside/api-client codegen` to regenerate from a
// running Django dev server (default http://localhost:8000/api/schema/).
//
// Until codegen runs the first time, this re-exports a placeholder type so
// downstream code can import without breaking.

export type ApiClientPlaceholder = {
  __notYetGenerated: true;
};

export const apiClientPlaceholder: ApiClientPlaceholder = { __notYetGenerated: true };
